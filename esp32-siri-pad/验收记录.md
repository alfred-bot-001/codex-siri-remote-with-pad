# 实施与验收记录

更新日期：2026-09-15（本地时间）。本文件随测试更新，未通过的项目不能视为完成。

## 已核实

- 第一版原生 ESP-IDF 固件编译、烧录及写入校验通过。
- Mac USB 树同时匹配 HID 键盘驱动和 USB Audio 控制／流接口。
- AVFoundation 音频设备列表出现 `Siri Voice Pad Microphone`。
- 用户确认实际开发板界面显示正常。
- C++ 状态逻辑 11 组情形测试通过，启用 AddressSanitizer 与 UndefinedBehaviorSanitizer。

## 修复与待验证

- 原截图功能调用图形库内部分配器，32 KB 内存池不足以容纳整屏，触发断言暂停；改为外部分配帧缓冲，并启用任务看门狗重启。已刷入，完整帧缓冲读取通过。
- 板载录音加入会话编号，快速停止／重启时拒绝上一次采集的样本。
- 原配对数据已另行备份并保留。原 Arduino 使用两个通用 MAC 地址，新 IDF 默认四个导致蓝牙地址改变；已恢复两个地址配置，并修正配对地址字符串大小写比较。冷启动后实测 `usb=1 ble=1`，自动连接成功。
- CoreAudio 直接读取指定 USB 麦克风：3.008 秒收到 144,000 个采样（48 kHz），待机峰值为零。此前 FFmpeg 录音长度偏短不能单独作为 USB 丢包证据。
- 遥控器与板载麦克风的受控说话测试、按键接收、触屏切换和微信输入法尚未完成验收。Mac 拒绝 HID 独占测试（返回 e00002e2），不能声称已在电脑端验证按键。

## 黑屏修复与现场验证

- 参考用户指定的“女佣任务面板”同板记录，恢复厂家上电重启步骤，屏幕复位等待改为 150 ms。
- 黑屏时程序仍运行，界面计数递增，LCD 复位为高、背光 PWM 仍在。单独恢复背光后用户确认仍黑；单独复位、初始化屏幕并重绘后，用户确认亮起。
- 已将成功的屏幕恢复流程放入全部界面和板载音频初始化结束处，USB 启动之前。新版刷入后，用户拔线重插确认“正常继续”。目前确认一次现场冷重插正常，尚不能声称长期稳定性已验证。

## 2026-09-15 遥控器麦克风复测

- 用户指出说话有主动停顿，因此保持固件不变，重新进行按住、说话、停顿、接着说话、松开的受控测试。
- CoreAudio 收到 2,880,000 个采样（60 秒音频），实耗 60.213 秒。回放只裁去首尾等待，保留内部停顿，未增加音量：`logs/remote-mic-retest.wav`，约 8.278 秒。
- 活跃期间 `source=1`；新增 373 帧，蓝牙丢包和解码错误增量均为 0；松开后 `source=0 voice=0 peak=0`。
- 缓冲丢弃计数由 724 增至 801（77 次）。这是内部缓冲事件，不能等同于 77 次可听断音，也不能将用户的主动停顿判断为断音。USB 发送计数在录音期间约每秒增加 971–978；待机采集约 1000。尚待区分传输调度、缓冲及实际听感影响。
- 本轮未修改或重刷固件；回放听感需要用户确认。

## 备份文件

- 出厂完整 16 MB 备份：`/Users/yafei/my-agents/esp32-siri-test/backups/original-16mb-1020ba4658b4.bin`。
- 升级前配对 NVS：`/Users/yafei/my-agents/esp32-siri-test/backups/paired-nvs-before-pad.bin`。
- 刷写时未写 NVS 区，也未执行全片擦除。

### 颜色配置

用户指出颜色异常后，逐项核对厂家 `ESP-IDF/02_lvgl_example/sdkconfig.defaults`：厂家启用 `CONFIG_LV_COLOR_16_SWAP=y`，本项目最初遗漏。已在默认配置和实际构建配置同时补齐，保留厂家 BGR 设置；修正版已烧录，用户确认颜色正常。

## GitHub 发布检查

- 2026-09-15：新增 ESP32 触屏固件、早期 Arduino 验证程序及树莓派原型到原有 Mac 应用仓库。
- 公开版中文字体改为 OFL 许可的 SiriPadCJK 子集，保留生成脚本与上游许可；没有因发布源码刷写现有设备。
- 公开目录从默认配置独立构建通过；11 组主机状态测试（AddressSanitizer / UndefinedBehaviorSanitizer）通过。新字体尚未在实体屏幕上目视验收。
- 现场录音、日志、Flash/NVS 备份及构建缓存不在提交中。

## 180° 旋转

- 按用户要求，通过 LVGL 将画面旋转 180°，触摸坐标由同一显示旋转配置处理，不额外反转触摸驱动。
- 当前设备固件编译、刷写校验通过；重启后 USB 与蓝牙均在线，屏幕刷新计数正常。保留原字体、颜色转换和屏幕恢复流程。
- USB 位置变化后，升级脚本支持显式指定固件／下载串口，先检查 USB 序列号，再检查下载模式芯片 MAC。
- 实际画面方向及点击位置等待用户现场确认。

## 应用快捷键与新布局

- 按用户提供布局新增 ChatGPT、Claude 两个应用按钮，底部改为左右箭头夹着大回车，下方增加输入法按钮；保留 180° 旋转和真实蓝牙、麦克风状态。
- 左／右遥控器按钮改为左 Ctrl＋左 Option＋左 Command＋左／右箭头；小电视键（报告位 0x0001）改为 Command＋空格；屏幕左右箭头仍移动光标。
- 增加单次组合键脉冲、语音修饰键隔离、松开后重新接收按键、队列空间不足拒绝完整快捷键等检查。17 组主机测试（ASan/UBSan）通过，固件编译通过。
- 用户确认已完成 Mac 快捷键绑定；新布局和电脑端唤起／隐藏、输入法切换仍待现场验证。没有实现图中未经回传的应用忙碌／输入法名称显示或排队发送。
- 新固件刷写校验通过，重启后 `usb=1 ble=1`；从设备读取完整帧缓冲，文字和按钮未见裁切。实体操作效果等待用户确认。

## 应用快捷键改为 G／C

- 用户确认 Mac 端已配置 Ctrl＋Option＋Command＋G（ChatGPT）、Ctrl＋Option＋Command＋C（Claude），固件同步为 HID G=0x0a、C=0x06，修饰键保持左 Ctrl／Option／Command=0x0d。
- 遥控器左／右和屏幕应用按钮发送新组合；屏幕底部光标左右键、输入法、回车和语音逻辑不变。17 组状态与快捷键测试通过。
- 已随紧凑布局版刷入目标 Pad，写入校验通过，USB 重新识别成功；G／C 快捷键电脑端效果待用户实测。

## 紧凑布局与蓝色回车

- 回车改蓝色，麦克风从 144×144 缩为 120×120，状态文字紧随图标；底部光标及回车键从 60 加高至 92 像素。
- 编译、刷写校验和 USB 重新枚举通过，已读取设备实际界面帧缓冲，未见裁切。屏幕保留 180° 旋转，用户目视及触摸验收待确认。

## 触摸无响应排查

- 用户报告点击屏幕无响应。USB 诊断仍可用，usb=1、ble=1；运行时间从 1232645 ms 增至 1249544 ms，界面 ticks 均为 9870。请求屏幕重绘返回 UI lock timeout，确认界面任务持锁阻塞；尚未定位到具体底层阻塞调用。
- 优化为显示状态变化时才更新对象，空闲仍更新心跳；新增独立界面心跳监测，停滞超过 10 秒停止语音并重启，启动日志保留恢复标记。此保护是故障恢复机制，不能视为根因已彻底解决。
- 修正版编译、刷写校验通过；重启后 USB 与蓝牙在线，两次诊断 ticks 从 328 增至 473，心跳间隔 89／18 ms，运行时间连续未重置。界面任务已恢复，实际触摸效果及长期稳定性待用户确认。

## 2026-09-24 播放／暂停键映射空格

- 遥控器 Play/Pause 报告位 0x0100 映射为标准键盘 Space（HID 0x2c），按下保持、松开释放；保留连接时抑制和断连释放。
- ASan/UBSan 主机测试 20 组通过，覆盖空格保持去重、松开、蓝牙与 USB 断连及重连时抑制；固件编译通过。
- 已刷入目标 10:20:ba:46:58:b4，写入校验通过，重启后 usb=1、ble=1，界面心跳正常。电脑端实际空格输入待用户按键验证。
- 上一版触摸修复后，用户已反馈触摸正常；长期稳定性继续观察。

## 2026-09-24 减号／加号映射光标方向键

- Volume Down（0x0004）映射左箭头（HID 0x50），Volume Up（0x0002）映射右箭头（HID 0x4f）；按下保持、松开释放，与屏幕方向键共用同一 HID 键位，避免重复报告。
- 20 组既有测试及新增方向键按住、屏幕重叠、蓝牙／USB 断连与重连抑制检查通过（ASan/UBSan）；编译通过。
- 固件已刷入并校验，重启后 USB、蓝牙在线，界面心跳正常。电脑端光标移动效果待用户实际按键确认。
